# Original Idea

This document preserves the original project wording.

> so I would like to create an extension that would allow us to record on forvo without using a mouse click to activate the record button. Now there were several ideas for that: either start the button after some time on it (customizable), use a keyboard button, or do a some sort of a gesture (like getting a circle on the button with the mouse to trigger it). Your take?
> Another feature: extension automatically copy pastes the word we are about to pronounce onto a specific field in https://www.goroh.pp.ua/ to trigger word search. It would be able to detect that website and perform that action automatically without hindering any process. Another option of that option: if it doesn't find the stress on any letter in that word (like here is stress present: ЧЕМОДА́НИЩЕ), it opens another (specified) tab in ChatGPT website and pastes the word there for lookup with ChatGPT.

